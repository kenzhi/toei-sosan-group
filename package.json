{
  "name": "toei-sosan-group",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "build": "astro build",
    "postbuild": "node scripts/generate-sitemap.mjs && node scripts/generate-og-images.mjs",
    "optimize-images": "node scripts/optimize-factory-images.mjs",
    "prebuild": "npm run optimize-images",
    "preview": "astro preview",
    "check": "astro check"
  },
  "dependencies": {
    "astro": "^4.16.18",
    "sharp": "^0.35.3"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.4",
    "typescript": "^5.6.3"
  }
}

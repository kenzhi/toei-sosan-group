---
import Layout from '../layouts/Layout.astro';
import Nav from '../components/Nav.astro';
import Footer from '../components/Footer.astro';
import { getCollection } from 'astro:content';
import { cleanSlug } from '../i18n/ui';

const capabilities = [
  { num: '01', icon: 'factory', title: 'OEM製造', desc: '広東省東莞を拠点とする自社工場群で、品質管理から量産対応まで一貫体制を構築。日本品質基準を中国製造で実現します。' },
  { num: '02', icon: 'globe',   title: '跨境電商', desc: '日本×中国×東南アジアの越境EC運営。Amazon・楽天・天猫・Shopeeを網羅し、物流・通関・カスタマーサービスを統合。' },
  { num: '03', icon: 'ship',    title: '国際貿易', desc: '埼玉本社を起点に、アジア全域のB2B貿易を展開。原材料調達から完成品輸出まで、サプライチェーン全域をカバー。' }
];

const areas = [
  { tag: 'MANUFACTURING', title: '精密製造 & 品質管理', desc: '自社工場による一貫生産体制',
    img: '/images/factory/上色 1 - heavy-blur.webp',
    href: '/factory/' },
  { tag: 'LOGISTICS', title: '国際物流 & サプライチェーン', desc: '埼玉⇄東莞の最適化されたルート構築',
    img: '/images/factory/仓储 1 - heavy-blur.webp',
    href: '/logistics/' },
  { tag: 'E-COMMERCE', title: '越境EC & デジタルマーケティング', desc: '多プラットフォーム統合運営',
    img: '/images/site/ecommerce.jpg',
    href: '/ecommerce/' },
  { tag: 'CONSULTING', title: '海外進出 & ビジネスコンサルティング', desc: '日中両国の法務・税務・市場参入支援',
    img: '/images/site/consulting.jpg',
    href: '/consulting/' }
];

// 最新 3 篇文章（cases + insights + news 合并，按日期降序）
const allArticles = [
  ...(await getCollection('cases', c => c.data.lang === 'ja' && !c.data.draft))
    .map(e => ({ ...e, kind: 'cases' as const, kindLabel: 'ケース' })),
  ...(await getCollection('insights', c => c.data.lang === 'ja' && !c.data.draft))
    .map(e => ({ ...e, kind: 'insights' as const, kindLabel: '業界知見' })),
  ...(await getCollection('news', c => c.data.lang === 'ja' && !c.data.draft))
    .map(e => ({ ...e, kind: 'news' as const, kindLabel: 'ニュース' })),
].sort((a, b) => b.data.date.valueOf() - a.data.date.valueOf()).slice(0, 3);

// Cover fallbacks
const defaultCovers: Record<string, string> = {
  cases: '/images/site/trade.jpg',
  insights: '/images/site/consulting.jpg',
  news: '/images/site/ecommerce.jpg',
};

// 最新 6 个产品（首页展示）
const featuredProducts = (await getCollection('products', p => p.data.lang === 'ja' && !p.data.draft))
  .sort((a, b) => b.data.date.valueOf() - a.data.date.valueOf())
  .slice(0, 6);

// FAQ
const faq = [
  { q: '初回相談は有料ですか？', a: '初回相談は無料です。課題のヒアリングとご提案まで、お気軽にお問い合わせください。' },
  { q: '対応エリアはどこまで？', a: '日本（埼玉県越谷）と中国（広東省東莞）の二拠点体制で、日本全国・中国全土・東南アジア全域に対応します。' },
  { q: '小ロット（試作）も可能ですか？', a: 'はい、試作・小ロットから量産まで対応可能です。OEM 製造は 100 個から、跨境電商は 1 SKU からご相談いただけます。' },
  { q: '契約から実行までの期間は？', a: '内容により異なりますが、最短 2 週間で着手可能な案件もあります。緊急案件は別途ご相談ください。' },
  { q: '品質管理体制はどうなっていますか？', a: '東莞工場に日本人駐在の品質管理チームを配置。ISO 9001 準拠の QC 工程、出荷前検査、サンプル試作時の寸法検査レポート提出まで一貫対応します。' },
  { q: 'NDA（秘密保持契約）は結べますか？', a: 'はい、初回相談前でも NDA 締結可能です。貴社の技術・顧客情報を保護した上で本音でお話しできます。' },
  { q: '支払い方法は？', a: '日本円・人民元・米ドルのいずれも対応。銀行振込（T/T）を基本とし、案件により L/C も承ります。' },
  { q: 'オンライン会議は対応可能ですか？', a: 'Zoom / Teams / Google Meet すべて対応。日本時間・中国時間どちらも調整可能です。' },
];

// FAQ schema
const faqSchema = {
  '@context': 'https://schema.org',
  '@type': 'FAQPage',
  mainEntity: faq.map(f => ({
    '@type': 'Question',
    name: f.q,
    acceptedAnswer: { '@type': 'Answer', text: f.a }
  }))
};
---
<Layout title="東栄創産グループ | 中国の工場から、世界へ。">
  <Nav />

  <!-- ═══════════ HERO ═══════════ -->
  <section class="hero">
    <div class="hero-bg-grid" aria-hidden="true"></div>
    <div class="hero-bg-accent" aria-hidden="true"></div>
    <div class="container hero-inner">
      <div class="hero-text">
        <div class="hero-eyebrow"><span class="eyebrow-line"></span><span class="eyebrow-text">JAPAN × CHINA × GLOBAL</span></div>
        <h1 class="hero-title">中国の工場から、<br/>世界へ。</h1>
        <p class="hero-sub">日本の品質基準と中国の製造力を融合し、アジアから世界市場へ価値を届ける実業集団。</p>
        <a href="/services/" class="hero-cta">EXPLORE MORE <span aria-hidden="true">→</span></a>
        <div class="hero-scroll" aria-hidden="true"><span></span><span class="scroll-label">SCROLL</span></div>
      </div>
      <div class="hero-visual" aria-hidden="true">
        <div class="hv-frame1"></div>
        <div class="hv-frame2"></div>
        <div class="hv-photo">
          <img src="/images/site/team.jpg" alt="" loading="eager" />
        </div>
        <div class="hv-corner tl"></div>
        <div class="hv-corner br"></div>
        <div class="hv-dot dot1"></div>
        <div class="hv-dot dot2"></div>
      </div>
    </div>
  </section>

  <!-- ═══════════ CORE CAPABILITIES ═══════════ -->
  <section class="caps section">
    <div class="container">
      <div class="sec-eyebrow"><span class="eyebrow-line"></span><span>CORE CAPABILITIES</span></div>
      <h2 class="sec-title">三つの核心事業で、<span class="t-accent">世界と繋がる</span></h2>
      <div class="caps-grid">
        {capabilities.map(c => (
          <article class="cap-card">
            <div class="cap-head">
              <span class="cap-num">{c.num}</span>
              <span class="cap-icon" data-icon={c.icon}></span>
            </div>
            <h3 class="cap-title">{c.title}</h3>
            <p class="cap-desc">{c.desc}</p>
            <div class="cap-more"><span class="dot"></span><span>DETAILS</span></div>
          </article>
        ))}
      </div>
    </div>
  </section>

  <!-- ═══════════ BUSINESS AREAS (2x2) ═══════════ -->
  <section class="areas section">
    <div class="container">
      <div class="sec-eyebrow"><span class="eyebrow-line"></span><span>BUSINESS AREAS</span></div>
      <h2 class="sec-title">事業領域</h2>
      <div class="areas-grid">
        {areas.map(a => (
          <a href={a.href} class="area-card">
            <img src={a.img} alt={a.title} loading="lazy"  decoding="async" />
            <div class="area-overlay"></div>
            <div class="area-content">
              <span class="area-tag">{a.tag}</span>
              <h3 class="area-title">{a.title}</h3>
              <p class="area-desc">{a.desc}</p>
            </div>
            <div class="area-corner"></div>
          </a>
        ))}
      </div>
    </div>
  </section>

  <!-- ═══════════ BRAND STORY (Asymmetric 2-col) ═══════════ -->
  <section class="story section">
    <div class="story-bg" aria-hidden="true">
      <svg viewBox="0 0 1440 800" preserveAspectRatio="xMidYMid slice">
        <path d="M0 400 Q360 300 720 400 T1440 400" stroke="#1B3A5C" stroke-width="0.8" fill="none" />
        <path d="M0 340 Q360 240 720 340 T1440 340" stroke="#1B3A5C" stroke-width="0.4" fill="none" />
        <path d="M0 460 Q360 360 720 460 T1440 460" stroke="#1B3A5C" stroke-width="0.4" fill="none" />
        <path d="M200 0 Q300 400 200 800" stroke="#E29C52" stroke-width="0.4" fill="none" />
        <path d="M600 0 Q500 400 600 800" stroke="#1B3A5C" stroke-width="0.3" fill="none" />
        <path d="M1000 0 Q1100 400 1000 800" stroke="#1B3A5C" stroke-width="0.3" fill="none" />
        <path d="M1300 0 Q1200 400 1300 800" stroke="#E29C52" stroke-width="0.4" fill="none" />
        <circle cx="350" cy="375" r="4" fill="#E29C52" opacity="0.5" />
        <circle cx="720" cy="400" r="3" fill="#1B3A5C" opacity="0.4" />
        <circle cx="1100" cy="425" r="4" fill="#E29C52" opacity="0.5" />
      </svg>
    </div>
    <div class="container story-inner">
      <div class="sec-eyebrow"><span class="eyebrow-line"></span><span>BRAND STORY</span></div>
      <div class="story-grid">
        <div class="story-left">
          <div class="story-photo">
            <img src="/images/site/team.jpg" alt="埼玉オフィス" loading="lazy"  decoding="async" />
            <div class="story-photo-frame"></div>
          </div>
          <blockquote class="story-quote">
            <p>"100%実業精神"</p>
            <cite>── 東栄創産の核心理念</cite>
          </blockquote>
        </div>
        <div class="story-right">
          <h2 class="story-title">埼玉県越谷 × 広東東莞<br/><span class="t-accent">二つの拠点、一つの精神</span></h2>
          <p>東栄創産グループは、埼玉県越谷市に本社を、広東省東莞市に生産拠点を構える日中一体型の実業集団です。2015 年から東莞工廠で磨いてきた OEM 製造の技を、2025 年の埼玉本社設立を機に、日本の企画力・品質基準と融合させ、アジアから世界市場へ価値を届けています。</p>
          <p>創業以来、「工場現場から離れない」という原則を堅持。物づくりの本質を見据えた経営で、パートナー企業とともにグローバルサプライチェーンの最適解を追求し続けます。</p>
          <div class="story-metrics">
            <div><span class="m-val" data-countup="2" data-countup-duration="1200">0</span><span class="m-suf">拠点</span><span class="m-lbl">SAITAMA × DONGGUAN</span></div>
            <div><span class="m-val" data-countup="3" data-countup-duration="1200">0</span><span class="m-suf">事業</span><span class="m-lbl">CORE BUSINESSES</span></div>
            <div><span class="m-val" data-countup="100" data-countup-duration="1800" data-countup-suffix="+">0</span><span class="m-suf">%</span><span class="m-lbl">実業精神</span></div>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- ═══════════ FAQ ═══════════ -->
  <section class="faq section">
    <div class="container narrow">
      <div class="sec-eyebrow"><span class="eyebrow-line"></span><span>FAQ</span></div>
      <h2 class="sec-title">よくある<span class="t-accent">ご質問</span></h2>
      <div class="faq-list">
        {faq.map((f, i) => (
          <details class="faq-item">
            <summary>
              <span class="faq-q-num">{String(i + 1).padStart(2, '0')}</span>
              <span class="faq-q">{f.q}</span>
              <span class="faq-icon" aria-hidden="true">+</span>
            </summary>
            <div class="faq-a">{f.a}</div>
          </details>
        ))}
      </div>
    </div>
  </section>

  <!-- ═══════════ PRODUCTS ═══════════ -->
  <section class="products-section section">
    <div class="container">
      <div class="products-section-header">
        <div class="sec-eyebrow"><span class="eyebrow-line"></span><span>PRODUCTS</span></div>
        <h2 class="sec-title">自社工場の<span class="t-accent">製品ラインアップ</span></h2>
        <p class="products-section-sub">金属加工・樹脂成形・電子組立・包装まで。<br />OEM / ODM 両対応、6 カテゴリから選定できます。</p>
      </div>
      <div class="products-section-grid">
        {featuredProducts.map(p => (
          <a href={`/products/${cleanSlug(p.slug)}/`} class="product-section-card">
            <div class="product-section-cover" style={`background-image: url(${(p.data as any).cover})`}>
              <span class="product-section-cat">{(p.data as any).category}</span>
            </div>
            <div class="product-section-content">
              <h3>{(p.data as any).title}</h3>
              <p>{(p.data as any).excerpt}</p>
            </div>
          </a>
        ))}
      </div>
      <div class="products-section-cta">
        <a href="/products/" class="btn-outline">すべての製品を見る →</a>
      </div>
    </div>
  </section>

  <!-- ═══════════ LATEST ARTICLES ═══════════ -->
  <section class="articles section">
    <div class="container">
      <div class="articles-header">
        <div class="sec-eyebrow"><span class="eyebrow-line"></span><span>LATEST</span></div>
        <h2 class="sec-title">最新の<span class="t-accent">知見と実績</span></h2>
      </div>
      <div class="articles-grid">
        {allArticles.map(article => {
          const cover = (article.data as any).cover || defaultCovers[article.kind];
          return (
            <a href={`/${article.kind}/${cleanSlug(article.slug)}/`} class="article-card">
              <div class="article-cover" style={`background-image: url(${cover})`}>
                <span class="article-kind">{article.kindLabel}</span>
                <div class="article-cover-overlay"></div>
              </div>
              <div class="article-content">
                <div class="article-meta">
                  <time>{article.data.date.toISOString().split('T')[0]}</time>
                  {article.data.industry && <span class="article-tag">{article.data.industry}</span>}
                  {article.data.category && <span class="article-tag">{article.data.category}</span>}
                </div>
                <h3 class="article-title">{article.data.title}</h3>
                <p class="article-excerpt">{article.data.excerpt}</p>
                <span class="article-link">記事を読む <span class="arrow">→</span></span>
              </div>
            </a>
          );
        })}
      </div>
    </div>
  </section>

  <!-- ═══════════ FINAL CTA ═══════════ -->
  <section class="final-cta section">
    <div class="container">
      <div class="cta-grid">
        <a href="/contact/" class="cta-box cta-primary">
          <span class="cta-eyebrow">お問い合わせ</span>
          <h3>プロジェクト、<br/>相談から承ります。</h3>
          <span class="cta-arrow">→</span>
        </a>
        <a href="/about/" class="cta-box">
          <span class="cta-eyebrow">私たちについて</span>
          <h3>なぜ「実業」か、<br/>その理由を聞く。</h3>
          <span class="cta-arrow">→</span>
        </a>
      </div>
    </div>
  </section>

  <Footer />

  <!-- FAQ JSON-LD schema -->
  <script type="application/ld+json" set:html={JSON.stringify(faqSchema)}></script>
</section>

<style>
  /* ────────── HERO ────────── */
  .hero { min-height: calc(100vh - 70px); display: flex; align-items: center; position: relative; overflow: hidden;
    background: linear-gradient(135deg, white 0%, var(--color-bg) 100%); padding: 4rem 0 6rem; }
  .hero-bg-grid { position: absolute; inset: 0; opacity: 0.03; pointer-events: none;
    background-image:
      linear-gradient(to right, var(--color-primary) 1px, transparent 1px),
      linear-gradient(to bottom, var(--color-primary) 1px, transparent 1px);
    background-size: 60px 60px; }
  .hero-bg-accent { position: absolute; top: 0; right: 0; width: 33%; height: 100%;
    background: linear-gradient(225deg, var(--color-primary-fade) 0%, transparent 70%); pointer-events: none; }
  .hero-inner { position: relative; z-index: 2; display: grid; grid-template-columns: 7fr 5fr; gap: 3rem; align-items: center; }
  .hero-text { position: relative; padding-top: 2rem; }
  .hero-eyebrow { display: flex; align-items: center; gap: 1rem; margin-bottom: 2rem;
    animation: fadeInUp 0.8s var(--ease) both; }
  .eyebrow-line { display: block; width: 64px; height: 3px; background: var(--color-accent); border-radius: 999px;
    animation: lineExpand 1s var(--ease) 0.4s both; }
  @keyframes lineExpand { from { width: 0; } to { width: 64px; } }
  .eyebrow-text { font-family: var(--font-en); font-size: 0.75rem; font-weight: 500; letter-spacing: 0.35em; color: var(--color-primary); opacity: 0.55; }
  .hero-title { font-size: clamp(2.5rem, 5.5vw, 4.5rem); font-weight: 700; line-height: 1.15; letter-spacing: -0.01em;
    color: var(--color-primary); margin: 0 0 1.5rem; animation: fadeInUp 0.8s var(--ease) 0.1s both; }
  @keyframes fadeInUp { from { opacity: 0; transform: translateY(24px); } to { opacity: 1; transform: translateY(0); } }
  .hero-sub { font-family: var(--font-sc); font-size: 1.0625rem; line-height: 1.85; color: var(--color-text-soft);
    max-width: 30rem; margin: 0 0 2.5rem; animation: fadeInUp 0.8s var(--ease) 0.2s both; }
  .hero-cta { display: inline-flex; align-items: center; gap: 0.5rem; padding: 0.9rem 2rem;
    font-family: var(--font-en); font-size: 0.75rem; font-weight: 600; letter-spacing: 0.25em;
    color: var(--color-primary); border: 2px solid var(--color-primary); border-radius: 2px;
    text-decoration: none; transition: var(--transition); animation: fadeInUp 0.8s var(--ease) 0.3s both; }
  .hero-cta:hover { background: var(--color-primary); color: white; transform: translateY(-2px);
    box-shadow: 0 8px 24px rgba(27, 58, 92, 0.25); }
  .hero-cta span { transition: transform 0.3s var(--ease); }
  .hero-cta:hover span { transform: translateX(4px); }
  .hero-scroll { display: flex; align-items: center; gap: 0.75rem; margin-top: 5rem; opacity: 0.5; }
  .hero-scroll span:first-child { display: block; width: 2px; height: 56px;
    background: linear-gradient(180deg, var(--color-primary) 0%, transparent 100%); border-radius: 999px; }
  .scroll-label { font-family: var(--font-en); font-size: 0.625rem; letter-spacing: 0.3em; color: var(--color-primary); opacity: 0.6; }

  /* Hero visual (right) */
  .hero-visual { position: relative; aspect-ratio: 1; max-width: 480px; margin: 0 auto; width: 100%;
    animation: floatSlow 7s ease-in-out infinite; }
  @keyframes floatSlow { 0%, 100% { transform: translateY(0) rotate(0deg); } 50% { transform: translateY(-10px) rotate(0.5deg); } }
  .hv-frame1 { position: absolute; top: 8%; right: 8%; width: 72%; height: 72%;
    border: 1px solid var(--color-primary); opacity: 0.2; transform: rotate(12deg); border-radius: 2px; }
  .hv-frame2 { position: absolute; top: 18%; right: 18%; width: 58%; height: 58%;
    background: var(--color-primary-fade); transform: rotate(-6deg); border-radius: 2px; }
  .hv-photo { position: absolute; top: 12%; left: 12%; width: 65%; height: 65%;
    border-radius: 2px; overflow: hidden; box-shadow: 0 24px 64px rgba(15, 23, 42, 0.18); }
  .hv-photo img { width: 100%; height: 100%; object-fit: cover; opacity: 0.85; }
  .hv-corner { position: absolute; width: 40px; height: 40px; }
  .hv-corner.tl { top: 0; left: 0; border-top: 2px solid var(--color-primary); border-left: 2px solid var(--color-primary); }
  .hv-corner.br { bottom: 0; right: 0; border-bottom: 2px solid var(--color-accent); border-right: 2px solid var(--color-accent); }
  .hv-dot { position: absolute; border-radius: 50%; }
  .hv-dot.dot1 { bottom: 18%; left: 3%; width: 12px; height: 12px; background: var(--color-accent);
    box-shadow: 0 4px 12px rgba(226, 156, 82, 0.4); }
  .hv-dot.dot2 { top: 5%; right: 5%; width: 8px; height: 8px; background: var(--color-primary); }

  /* ────────── SECTION EYEBROW ────────── */
  .sec-eyebrow { display: flex; align-items: center; gap: 1rem; margin-bottom: 2rem; }
  .sec-eyebrow .eyebrow-line { width: 32px; height: 2px; animation: none; }
  .sec-eyebrow span:last-child { font-family: var(--font-en); font-size: 0.6875rem; font-weight: 600;
    letter-spacing: 0.35em; color: var(--color-primary); opacity: 0.6; text-transform: uppercase; }
  .sec-title { font-size: clamp(1.75rem, 3.5vw, 2.5rem); font-weight: 600; color: var(--color-primary);
    line-height: 1.3; margin: 0 0 3.5rem; }
  .t-accent { color: var(--color-accent); }

  /* ────────── CORE CAPABILITIES ────────── */
  .caps { background: var(--color-bg); }
  .caps-grid { display: grid; grid-template-columns: repeat(3, 1fr); gap: 1.5rem; }
  .cap-card { background: white; border: 1px solid transparent; border-radius: var(--radius-lg);
    padding: 2.5rem 2rem; transition: var(--transition); cursor: pointer; }
  .cap-card:hover { border-color: var(--color-accent);
    box-shadow: 0 8px 32px rgba(27, 58, 92, 0.08), 0 0 0 1px var(--color-accent); transform: translateY(-4px); }
  .cap-head { display: flex; align-items: center; gap: 1rem; margin-bottom: 1.5rem; }
  .cap-num { font-family: var(--font-display); font-size: 2.75rem; font-weight: 400; line-height: 1;
    color: var(--color-primary); opacity: 0.15; transition: color 0.4s var(--ease); }
  .cap-card:hover .cap-num { color: var(--color-accent); }
  .cap-icon { width: 40px; height: 40px; border-radius: 50%; background: var(--color-primary-fade);
    display: flex; align-items: center; justify-content: center; font-size: 1.1rem; color: var(--color-primary);
    opacity: 0.6; transition: var(--transition); }
  .cap-card:hover .cap-icon { background: var(--color-primary); opacity: 0.9; }
  .cap-title { font-size: 1.5rem; font-weight: 600; color: var(--color-primary); margin: 0 0 1rem; }
  .cap-desc { font-family: var(--font-sc); font-size: 0.875rem; line-height: 1.85; color: var(--color-text-soft);
    margin: 0; }
  .cap-more { display: flex; align-items: center; gap: 0.5rem; margin-top: 1.75rem;
    opacity: 0; transition: opacity 0.4s var(--ease); }
  .cap-card:hover .cap-more { opacity: 1; }
  .cap-more .dot { width: 32px; height: 2px; background: var(--color-accent); border-radius: 999px; }
  .cap-more span:last-child { font-family: var(--font-en); font-size: 0.625rem; font-weight: 600;
    letter-spacing: 0.2em; color: var(--color-accent); }

  /* ────────── BUSINESS AREAS (2x2 image grid) ────────── */
  .areas { background: white; }
  .areas-grid { display: grid; grid-template-columns: 1fr 1fr; gap: 1.5rem; }
  .area-card { position: relative; aspect-ratio: 4/3; overflow: hidden; border-radius: var(--radius-lg);
    background: var(--color-bg-soft); display: block; text-decoration: none; }
  .area-card img { width: 100%; height: 100%; object-fit: cover; filter: grayscale(1); opacity: 0.75;
    transition: all 0.7s var(--ease); }
  .area-card:hover img { filter: grayscale(0); opacity: 1; transform: scale(1.05); }
  .area-overlay { position: absolute; inset: 0;
    background: linear-gradient(0deg, rgba(27, 58, 92, 0.7) 0%, rgba(27, 58, 92, 0.2) 50%, transparent 100%); }
  .area-content { position: absolute; bottom: 1.5rem; left: 1.5rem; right: 1.5rem; }
  .area-tag { font-family: var(--font-en); font-size: 0.625rem; font-weight: 600; letter-spacing: 0.3em;
    color: var(--color-accent); text-transform: uppercase; }
  .area-title { font-family: var(--font-display); font-size: 1.25rem; font-weight: 600; color: white;
    margin: 0.5rem 0 0.4rem; line-height: 1.3; }
  .area-desc { font-family: var(--font-sc); font-size: 0.75rem; color: rgba(255, 255, 255, 0.75); margin: 0; }
  .area-corner { position: absolute; top: 1rem; right: 1rem; width: 24px; height: 24px;
    border-top: 2px solid var(--color-accent); border-right: 2px solid var(--color-accent);
    opacity: 0; transition: opacity 0.4s var(--ease); border-radius: 1px; }
  .area-card:hover .area-corner { opacity: 1; }

  /* ────────── BRAND STORY ────────── */
  .story { background: var(--color-bg); position: relative; overflow: hidden; }
  .story-bg { position: absolute; inset: 0; opacity: 0.04; pointer-events: none; }
  .story-bg svg { width: 100%; height: 100%; }
  .story-inner { position: relative; z-index: 2; }
  .story-grid { display: grid; grid-template-columns: 2fr 3fr; gap: 4rem; align-items: start; }
  .story-photo { position: relative; border-radius: var(--radius-lg); overflow: hidden;
    box-shadow: 0 16px 48px rgba(15, 23, 42, 0.12); }
  .story-photo img { width: 100%; height: auto; aspect-ratio: 3/4; object-fit: cover; display: block; }
  .story-photo-frame { position: absolute; inset: 1rem; border: 1px solid rgba(255, 255, 255, 0.3);
    border-radius: 2px; pointer-events: none; }
  .story-quote { margin: 2rem 0 0 1.5rem; padding-left: 1.5rem; border-left: 3px solid var(--color-accent); }
  .story-quote p { font-family: var(--font-display); font-size: 1.375rem; font-style: italic; color: var(--color-primary);
    line-height: 1.5; margin: 0; }
  .story-quote cite { display: block; margin-top: 0.5rem; font-family: var(--font-sc); font-size: 0.75rem;
    letter-spacing: 0.05em; color: var(--color-text-mute); font-style: normal; }
  .story-right { padding-top: 2rem; }
  .story-title { font-size: clamp(1.75rem, 3.5vw, 2.5rem); font-weight: 600; line-height: 1.3; margin: 0 0 2rem; }
  .story-right p { font-family: var(--font-sc); font-size: 0.9375rem; line-height: 2; color: var(--color-text-soft);
    margin: 0 0 1.5rem; }
  .story-metrics { display: grid; grid-template-columns: repeat(3, 1fr); gap: 1.5rem; margin-top: 2.5rem;
    padding-top: 2rem; border-top: 1px solid var(--color-border); }
  .story-metrics > div { display: flex; flex-direction: column; gap: 0.25rem; }
  .m-val { font-family: var(--font-display); font-size: 2.5rem; font-weight: 700; color: var(--color-primary); line-height: 1; }
  .m-suf { font-family: var(--font-sc); font-size: 0.875rem; color: var(--color-accent); margin-left: 0.15rem; }
  .m-lbl { font-family: var(--font-en); font-size: 0.625rem; letter-spacing: 0.15em; color: var(--color-text-mute);
    text-transform: uppercase; margin-top: 0.5rem; }

  /* ────────── FAQ ────────── */
  .faq { background: white; }
  .faq .narrow { max-width: 800px; }
  .faq-list { display: flex; flex-direction: column; gap: 0.75rem; }
  .faq-item {
    background: var(--color-bg);
    border: 1px solid var(--color-border-soft);
    border-radius: var(--radius-lg);
    transition: all 0.3s var(--ease);
  }
  .faq-item[open] {
    background: white;
    border-color: var(--color-accent);
    box-shadow: 0 4px 16px rgba(15, 23, 42, 0.06);
  }
  .faq-item summary {
    display: flex;
    align-items: center;
    gap: 1.25rem;
    padding: 1.5rem 1.75rem;
    cursor: pointer;
    list-style: none;
    user-select: none;
  }
  .faq-item summary::-webkit-details-marker { display: none; }
  .faq-q-num {
    font-family: var(--font-en);
    font-size: 0.75rem;
    font-weight: 600;
    color: var(--color-accent);
    letter-spacing: 0.1em;
    flex-shrink: 0;
    width: 2rem;
  }
  .faq-q {
    font-family: var(--font-sc);
    font-size: 1rem;
    font-weight: 500;
    color: var(--color-primary);
    flex: 1;
  }
  .faq-icon {
    width: 28px;
    height: 28px;
    border: 1.5px solid var(--color-border);
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 1.25rem;
    font-weight: 300;
    color: var(--color-primary);
    transition: all 0.3s var(--ease);
    flex-shrink: 0;
  }
  .faq-item[open] .faq-icon {
    background: var(--color-accent);
    border-color: var(--color-accent);
    color: white;
    transform: rotate(45deg);
  }
  .faq-a {
    padding: 0 1.75rem 1.5rem 4.75rem;
    font-family: var(--font-sc);
    font-size: 0.9375rem;
    line-height: 1.85;
    color: var(--color-text-soft);
  }
  @media (max-width: 640px) {
    .faq-item summary { padding: 1.25rem; gap: 0.75rem; }
    .faq-q-num { width: 1.5rem; }
    .faq-a { padding: 0 1.25rem 1.25rem 2.75rem; }
  }

  /* ────────── PRODUCTS SECTION ────────── */
  .products-section { background: white; }
  .products-section-header { margin-bottom: 3rem; text-align: center; }
  .products-section-header .sec-eyebrow { justify-content: center; }
  .products-section-sub {
    font-family: var(--font-sc);
    font-size: 1rem;
    line-height: 1.8;
    color: var(--color-text-soft);
    margin: 1rem auto 0;
    max-width: 600px;
  }
  .products-section-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 1.75rem;
  }
  .product-section-card {
    display: block;
    background: white;
    border: 1px solid var(--color-border-soft);
    border-radius: var(--radius-lg);
    overflow: hidden;
    text-decoration: none;
    color: var(--color-primary);
    transition: all 0.3s var(--ease);
  }
  .product-section-card:hover {
    transform: translateY(-4px);
    box-shadow: 0 12px 32px rgba(15, 23, 42, 0.1);
    border-color: var(--color-accent);
  }
  .product-section-cover {
    position: relative;
    aspect-ratio: 16/10;
    background-size: cover;
    background-position: center;
    background-color: var(--color-bg-soft);
  }
  .product-section-cat {
    position: absolute;
    top: 0.875rem; left: 0.875rem;
    font-family: var(--font-en);
    font-size: 0.625rem;
    font-weight: 600;
    letter-spacing: 0.2em;
    color: var(--color-accent);
    text-transform: uppercase;
    background: rgba(255, 255, 255, 0.95);
    padding: 0.3rem 0.7rem;
    border-radius: 2px;
  }
  .product-section-content { padding: 1.5rem 1.75rem 1.75rem; }
  .product-section-content h3 {
    font-family: var(--font-display);
    font-size: 1.0625rem;
    font-weight: 600;
    line-height: 1.4;
    margin: 0 0 0.5rem;
  }
  .product-section-content p {
    font-family: var(--font-sc);
    font-size: 0.8125rem;
    line-height: 1.6;
    color: var(--color-text-soft);
    margin: 0;
    display: -webkit-box;
    -webkit-line-clamp: 2;
    -webkit-box-orient: vertical;
    overflow: hidden;
  }
  .products-section-cta {
    text-align: center;
    margin-top: 3rem;
  }
  .btn-outline {
    display: inline-block;
    padding: 0.75rem 1.75rem;
    font-family: var(--font-en);
    font-size: 0.75rem;
    font-weight: 600;
    letter-spacing: 0.18em;
    text-transform: uppercase;
    color: var(--color-primary);
    border: 1.5px solid var(--color-primary);
    border-radius: 2px;
    text-decoration: none;
    transition: all 0.3s var(--ease);
  }
  .btn-outline:hover { background: var(--color-primary); color: white; }

  /* ────────── LATEST ARTICLES ────────── */
  .articles { background: var(--color-bg); }
  .articles-header { margin-bottom: 3rem; }
  .articles-grid { display: grid; grid-template-columns: repeat(3, 1fr); gap: 2rem; }
  .article-card {
    display: block;
    background: white;
    border: 1px solid var(--color-border-soft);
    border-radius: var(--radius-lg);
    overflow: hidden;
    text-decoration: none;
    color: var(--color-primary);
    transition: all 0.3s var(--ease);
  }
  .article-card:hover {
    transform: translateY(-4px);
    box-shadow: 0 12px 32px rgba(15, 23, 42, 0.1);
    border-color: var(--color-accent);
  }
  .article-cover {
    position: relative;
    aspect-ratio: 16/10;
    background-size: cover;
    background-position: center;
    background-color: var(--color-bg-soft);
  }
  .article-cover-overlay {
    position: absolute; inset: 0;
    background: linear-gradient(180deg, transparent 50%, rgba(15, 23, 42, 0.4) 100%);
  }
  .article-kind {
    position: absolute;
    top: 0.875rem; left: 0.875rem;
    font-family: var(--font-en);
    font-size: 0.625rem;
    font-weight: 600;
    letter-spacing: 0.2em;
    text-transform: uppercase;
    color: var(--color-accent);
    background: rgba(255, 255, 255, 0.95);
    padding: 0.3rem 0.7rem;
    border-radius: 2px;
  }
  .article-content { padding: 1.5rem 1.75rem 1.75rem; }
  .article-meta {
    display: flex;
    gap: 0.5rem;
    align-items: center;
    margin-bottom: 0.75rem;
    flex-wrap: wrap;
  }
  .article-meta time {
    font-family: var(--font-en);
    font-size: 0.6875rem;
    color: var(--color-text-mute);
    letter-spacing: 0.1em;
  }
  .article-tag {
    font-family: var(--font-en);
    font-size: 0.625rem;
    color: var(--color-accent);
    letter-spacing: 0.1em;
    text-transform: uppercase;
  }
  .article-title {
    font-family: var(--font-display);
    font-size: 1.125rem;
    font-weight: 600;
    line-height: 1.35;
    margin: 0 0 0.5rem;
  }
  .article-excerpt {
    font-family: var(--font-sc);
    font-size: 0.8125rem;
    line-height: 1.6;
    color: var(--color-text-soft);
    margin: 0 0 1rem;
    display: -webkit-box;
    -webkit-line-clamp: 2;
    -webkit-box-orient: vertical;
    overflow: hidden;
  }
  .article-link {
    font-family: var(--font-en);
    font-size: 0.75rem;
    font-weight: 600;
    color: var(--color-accent);
    letter-spacing: 0.05em;
  }
  .article-link .arrow { display: inline-block; transition: transform 0.3s var(--ease); }
  .article-card:hover .article-link .arrow { transform: translateX(4px); }

  /* ────────── FINAL CTA ────────── */
  .final-cta { background: white; padding-bottom: 6rem; }
  .cta-grid { display: grid; grid-template-columns: 1fr 1fr; gap: 1.5rem; }
  .cta-box { position: relative; padding: 2.5rem; background: var(--color-bg); border-radius: var(--radius-lg);
    border: 1px solid transparent; transition: var(--transition); text-decoration: none; overflow: hidden; }
  .cta-box:hover { background: var(--color-primary); border-color: var(--color-primary); }
  .cta-eyebrow { font-family: var(--font-en); font-size: 0.625rem; font-weight: 600; letter-spacing: 0.3em;
    color: var(--color-accent); text-transform: uppercase; }
  .cta-box:hover .cta-eyebrow { color: var(--color-accent-soft); }
  .cta-box h3 { font-size: 1.375rem; font-weight: 600; color: var(--color-primary); margin: 0.75rem 0 0; line-height: 1.45; }
  .cta-box:hover h3 { color: white; }
  .cta-arrow { position: absolute; bottom: 1.5rem; right: 1.5rem; font-size: 1.5rem; color: var(--color-primary);
    opacity: 0.4; transition: all 0.4s var(--ease); }
  .cta-box:hover .cta-arrow { color: var(--color-accent); opacity: 1; transform: translate(4px, -4px); }
  .cta-primary { background: var(--color-primary); }
  .cta-primary .cta-eyebrow { color: var(--color-accent); }
  .cta-primary h3 { color: white; }
  .cta-primary .cta-arrow { color: var(--color-accent); opacity: 0.8; }
  .cta-primary:hover { background: var(--color-accent); }
  .cta-primary:hover .cta-eyebrow { color: white; }
  .cta-primary:hover .cta-arrow { color: white; }

  /* ────────── RESPONSIVE ────────── */
  @media (max-width: 1023px) {
    .hero-inner { grid-template-columns: 1fr; }
    .hero-visual { max-width: 360px; margin-top: 2rem; }
    .hero-scroll { display: none; }
    .caps-grid { grid-template-columns: 1fr; }
    .story-grid { grid-template-columns: 1fr; gap: 2.5rem; }
    .story-right { padding-top: 0; }
  }
  @media (max-width: 640px) {
    .areas-grid, .cta-grid, .articles-grid, .products-section-grid { grid-template-columns: 1fr; }
    .story-metrics { grid-template-columns: 1fr; gap: 1.25rem; }
  }
</style>

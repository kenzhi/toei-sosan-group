---
import Layout from '../../layouts/Layout.astro';
import Nav from '../../components/Nav.astro';
import Footer from '../../components/Footer.astro';
import PageHero from '../../components/PageHero.astro';

const platforms = [
  { region: 'Japan', items: ['Amazon JP', 'Rakuten', 'Yahoo! Shopping', 'Shopify Plus', 'Qoo10 JAPAN'] },
  { region: 'China', items: ['Tmall', 'JD.com', 'Pinduoduo', 'Douyin E-commerce', 'Xiaohongshu'] },
  { region: 'Southeast Asia', items: ['Shopee', 'Lazada', 'Tokopedia', 'TikTok Shop'] },
  { region: 'Cross-border', items: ['Amazon US / EU', 'AliExpress', 'eBay', 'TikTok Shop Global'] }
];

const services = [
  { num: '01', title: 'Listing & Store Setup', desc: 'Platform-compliant product pages with multi-language support, image production, and SEO optimization.' },
  { num: '02', title: 'Fulfillment & FBA', desc: 'Direct shipping from our Dongguan factory and partner warehouses to FBA worldwide. Japan FBA / cross-border FBA in as fast as 7 days.' },
  { num: '03', title: 'Customer Support', desc: 'Tri-lingual support in Japanese, Chinese, and English. Review responses, returns, and Q&A — 365 days a year.' },
  { num: '04', title: 'Advertising & CRM', desc: 'Amazon Ads / Rakuten RMS / Douyin ad buying. CRM and email design focused on LTV maximization.' }
];

const serviceSchema = {
  '@context': 'https://schema.org',
  '@type': 'Service',
  '@id': 'https://toei-sosan.com/en/ecommerce/#service',
  name: 'Cross-border E-commerce & Digital Marketing',
  description: '15+ platforms: Amazon, Rakuten, Tmall, Shopee. Fulfillment, customer support, and ad operations — all in one stop.',
  provider: { '@type': 'Organization', name: 'Toei Sosan Group Co., Ltd.', url: 'https://toei-sosan.com/en/' },
  areaServed: ['JP', 'CN', 'US', 'EU', 'ASEAN'],
  serviceType: 'Cross-border E-commerce Operations'
};

const breadcrumbSchema = {
  '@context': 'https://schema.org',
  '@type': 'BreadcrumbList',
  itemListElement: [
    { '@type': 'ListItem', position: 1, name: 'Home', item: 'https://toei-sosan.com/en/' },
    { '@type': 'ListItem', position: 2, name: 'Cross-border E-commerce', item: 'https://toei-sosan.com/en/ecommerce/' }
  ]
};
---
<Layout title="Cross-border E-commerce | Toei Sosan Group" description="15+ platforms: Amazon, Rakuten, Tmall, Shopee. Fulfillment, customer support, and ad operations — all in one stop." lang="en">
  <script type="application/ld+json" set:html={JSON.stringify(serviceSchema)} is:inline />
  <script type="application/ld+json" set:html={JSON.stringify(breadcrumbSchema)} is:inline />

  <Nav />

  <PageHero
    eyebrow="E-COMMERCE"
    title="Cross-border E-commerce"
    subtitle="15+ platforms. Integrated fulfillment, support, and advertising for maximum gross sales."
  />

  <section class="section">
    <div class="container">
      <div class="sec-eyebrow"><span class="eyebrow-line"></span><span>PLATFORMS</span></div>
      <h2 class="sec-title">Supported Platforms</h2>
      <div class="platforms-grid">
        {platforms.map(p => (
          <div class="platform-card">
            <div class="platform-region">{p.region}</div>
            <ul class="platform-list">
              {p.items.map(i => <li>{i}</li>)}
            </ul>
          </div>
        ))}
      </div>
    </div>
  </section>

  <section class="section services-section">
    <div class="container">
      <div class="sec-eyebrow"><span class="eyebrow-line"></span><span>SERVICES</span></div>
      <h2 class="sec-title">What We Do</h2>
      <div class="services-grid">
        {services.map(s => (
          <div class="service-card">
            <div class="service-num">{s.num}</div>
            <h3 class="service-title">{s.title}</h3>
            <p class="service-desc">{s.desc}</p>
          </div>
        ))}
      </div>
    </div>
  </section>

  <section class="section">
    <div class="container">
      <div class="cta-banner">
        <h3>Cross-border e-commerce,<br />from strategy to operations.</h3>
        <a href="/en/contact/" class="btn btn-primary">Contact Us</a>
      </div>
    </div>
  </section>

  <Footer />
</Layout>

<style>
  .sec-eyebrow { display: flex; align-items: center; gap: 0.75rem; color: var(--color-accent); font-family: var(--font-en); font-size: 0.75rem; font-weight: 600; letter-spacing: 0.35em; margin-bottom: 1rem; }
  .eyebrow-line { width: 32px; height: 1px; background: var(--color-accent); }
  .sec-title { font-family: var(--font-display); font-size: clamp(1.75rem, 3vw, 2.5rem); color: var(--color-primary); margin: 0 0 2.5rem; font-weight: 600; }
  .platforms-grid { display: grid; grid-template-columns: repeat(4, 1fr); gap: 1.5rem; }
  .platform-card { background: var(--color-bg-white); border: 1px solid var(--color-border-soft); border-top: 3px solid var(--color-accent); padding: 1.75rem 1.5rem; }
  .platform-region { font-family: var(--font-en); font-size: 0.75rem; color: var(--color-accent); letter-spacing: 0.2em; margin-bottom: 1rem; font-weight: 600; }
  .platform-list { list-style: none; padding: 0; margin: 0; }
  .platform-list li { padding: 0.5rem 0; border-bottom: 1px dashed var(--color-border-soft); color: var(--color-primary); font-size: 0.9375rem; }
  .platform-list li:last-child { border-bottom: none; }
  .services-section { background: var(--color-bg); }
  .services-grid { display: grid; grid-template-columns: repeat(2, 1fr); gap: 2rem; }
  .service-card { background: var(--color-bg-white); border: 1px solid var(--color-border-soft); padding: 2rem; }
  .service-num { font-family: var(--font-display); font-size: 2rem; color: var(--color-accent); font-weight: 600; line-height: 1; margin-bottom: 0.75rem; }
  .service-title { font-size: 1.25rem; color: var(--color-primary); margin: 0 0 0.75rem; font-weight: 600; }
  .service-desc { color: var(--color-text-soft); line-height: 1.8; font-size: 0.9375rem; margin: 0; }
  .cta-banner { background: var(--color-primary); color: white; padding: 3.5rem 2.5rem; text-align: center; }
  .cta-banner h3 { color: white; margin-bottom: 2rem; line-height: 1.5; }
  @media (max-width: 1024px) {
    .platforms-grid { grid-template-columns: repeat(2, 1fr); }
    .services-grid { grid-template-columns: 1fr; }
  }
</style>
